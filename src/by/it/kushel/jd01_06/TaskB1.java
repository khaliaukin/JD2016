package by.it.kushel.jd01_06;

import java.util.Map;
import java.util.TreeMap;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

/**
 * Created by Diomn on 14.05.2016.
 */
public class TaskB1 {
    /* public static void main(String[] arg) {
        char symbol='у';
        Pattern pat;
        Pattern pat2;
        Matcher mat;
        StringBuffer str = new StringBuffer(Data.lukomor);
        pat = Pattern.compile("(^|[^А-яа-яЁё])([А-Яа-яЁё]++)");
        pat2 = Pattern.compile("[б|в|г|д|ж|з|к|л|м|н|п|р|с|т|ф|х|ц|ч|ш|щ.+]{5,}]");
        mat = pat.matcher(str);


        while (mat.find()) {
            int indexOfWordStart = mat.start();
            int indexOfEnd = mat.end();
            str.delete(indexOfWordStart, indexOfEnd);
            }
        }

*/
}
